# GLM51 JSONL Viewer

Single-file browser viewer for ShareGPT/GLM51 SFT JSONL and ATIF/Pi trajectory data.

Open `index.html` in a browser or use the GitHub Pages deployment for local file-based viewing.
